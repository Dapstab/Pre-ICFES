extends ../dashboard
//-each course in user.courses
block contentDashboard
    .courses-container
        ul.courses(data-role=user.rol)
            if user.rol==="estudiante"
                //-button Unirse a Curso
                //-form.modal.join-course.hidden
                form.join-course
                    label(for="course-code") Código del Curso
                    input(type="text" id="course-code")
                    button.btn.btn-small.join-course Unirse
            else
                a.btn-small.create-course(href="/course") Crear Curso
            
            each course, i in courses 
                li.course
                    a.course-link(href=`course/${course._id}`, data-courseid=course._id)= course.nombre
        
        
        